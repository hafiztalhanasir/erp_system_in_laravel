<div class="col-md-12">
    <div id="nestable-menu">
        <a href="{{route('dashboard')}}" class="btn btn-white btn-sm">Dashboard</a>
        <a href="{{route('employee.index')}}" class="btn btn-white btn-sm">All Employees</a>
        <a href="{{route('notice_board.index')}}" class="btn btn-white btn-sm">Notice Board</a>
        <a href="{{route('calendar.index')}}" class="btn btn-white btn-sm">Vacations / Holiday calendar</a>
        <a href="{{route('leave.index')}}" class="btn btn-white btn-sm">Leave</a>
        <a href="{{route('expense.index')}}" class="btn btn-white btn-sm">Expenses</a>
        <a href="{{route('complaint.index')}}" class="btn btn-white btn-sm">Employee request & complaints </a>
        <a href="{{route('attendance.index')}}" class="btn btn-white btn-sm">Attendance</a>
    </div>
</div>
